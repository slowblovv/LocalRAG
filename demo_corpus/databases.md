# Databases

A database transaction groups operations with atomic commit semantics.

## Isolation

Isolation controls how concurrent transactions observe each other's writes. Stronger isolation can reduce concurrency or increase coordination cost.

Indexes accelerate reads by maintaining additional structures, but they consume storage and can make writes more expensive.
