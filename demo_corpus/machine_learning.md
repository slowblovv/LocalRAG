# Machine Learning

Supervised learning maps inputs to labels using a training set. Classification outputs classes or probabilities, while regression estimates continuous values.

## Evaluation

A held-out test set estimates generalization. Precision, recall, PR-AUC and ROC-AUC answer different questions and should be chosen for the error costs and class balance.
