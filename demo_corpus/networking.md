# Networking

TCP provides reliable, ordered byte-stream delivery between endpoints. It uses sequence numbers, acknowledgements and retransmission to recover from packet loss.

## Latency

Network latency includes propagation, transmission, processing and queueing delays. Tail latency such as p95 and p99 can matter more than the mean for user-facing services.
