export default function Docs(){return <main className="wrap"><h1>LocalRAG architecture</h1><div className="card"><pre>{`Documents → parsing → deterministic chunks\n                 ├─ TinySearch/BM25\n                 └─ embeddings → vector index\n                         ↓\n                    RRF hybrid\n                         ↓\n                     reranker\n                         ↓\n                  context builder\n                         ↓\n                    local LLM\n                         ↓\n             validated answer + citations`}</pre></div><div className="card"><p>Retrieved document text is treated as untrusted data. The system does not expose tool execution to documents. Citation IDs are validated against the retrieved context.</p></div></main>}
